# bc calculator startup file
# loads via environmental variable BC_ENV_ARGS
# see .bash_profile

# use four decimal places
scale=4

# convert temperatures fahrenheit and celsius
define c(t) { scale=4; return (t-32)*5/9; }
define f(t) { scale=4; return (t*9/5+32); }
